=form_for([@post, @post.comments.build]) do |f|
  %p
    = f.label :commenter, 'コメンター'
    %br
    = f.text_field :commenter
  %p
    = f.label :body, 'コメント'
    %br
    = f.text_area :body, cols: 60, rows: 8
  %p= f.submit 'コメント投稿'
