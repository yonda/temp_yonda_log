%h1 すべての投稿一覧

%table
  %tr
    %th タイトル
    %th 内容

  - @posts.each do |post|
    %tr
      %td= post.title
      %td= post.text
      %td= link_to '詳細', post
      %td= link_to '編集', edit_post_path(post)
      %td= link_to '削除', post_path(post), method: :delete, data: {confirm: '削除しますか？'}
= link_to 'New Post', new_post_path
